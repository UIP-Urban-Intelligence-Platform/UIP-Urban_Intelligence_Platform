# SPDX-License-Identifier: MIT
# Copyright (c) 2025 Nguyen Nhat Quang
#
# UIP - Urban Intelligence Platform
# GitHub Actions Workflow: Code Quality
#
# Module: .github/workflows/lint.yml
# Author: Nguyen Nhat Quang (Lead), Nguyen Viet Hoang, Nguyen Dinh Anh Tuan
# Version: 1.0.0

name: Code Quality

on:
  push:
    branches: [ main, develop, Quang ]
  pull_request:
    branches: [ main, develop ]
  workflow_dispatch:

jobs:
  lint:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.11'
        cache: 'pip'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements/dev.txt

    - name: Check code formatting with Black
      run: |
        black --check --diff src/ tests/

    - name: Check import sorting with isort
      run: |
        isort --check-only --diff src/ tests/

    - name: Lint with flake8
      run: |
        flake8 src/ tests/ --count --statistics

    - name: Type check with mypy
      run: |
        mypy src/ --ignore-missing-imports --exclude site-packages || true
      continue-on-error: true

    - name: Security check with bandit
      run: |
        bandit -r src/ -f json -o bandit-report.json || true
        bandit -r src/ -ll || true
      continue-on-error: true

    - name: Check docstring coverage
      run: |
        interrogate src/ --fail-under=50 -vv

    - name: Archive lint reports
      if: always()
      uses: actions/upload-artifact@v4
      with:
        name: lint-reports
        path: |
          bandit-report.json
        retention-days: 30
